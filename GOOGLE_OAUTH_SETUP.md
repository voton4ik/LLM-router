# 🔐 Настройка Google OAuth для OnepromptAI

Это руководство поможет вам настроить вход через Google в вашем приложении.

---

## 📋 Шаг 1: Создание проекта в Google Cloud Console

### 1.1 Создайте Google Cloud проект

1. Перейдите на [Google Cloud Console](https://console.cloud.google.com/)
2. Нажмите **Select a project** → **New Project**
3. Введите название проекта: `OnepromptAI` (или любое другое)
4. Нажмите **Create**

### 1.2 Настройте OAuth Consent Screen

1. В боковом меню выберите **APIs & Services** → **OAuth consent screen**
2. Выберите **External** (для публичного использования)
3. Нажмите **Create**

4. **Заполните обязательные поля:**
   - **App name:** `OnepromptAI` (или название вашего приложения)
   - **User support email:** выберите ваш email
   - **Developer contact email:** введите ваш email

5. Нажмите **Save and Continue**

6. **Scopes (области доступа):**
   - Нажмите **Add or Remove Scopes**
   - Выберите следующие scopes:
     - `openid`
     - `profile`
     - `email`
   - Нажмите **Update** → **Save and Continue**

7. **Test users (для режима Testing):**
   - Добавьте ваш email для тестирования
   - Нажмите **Add** → **Save and Continue**

8. Нажмите **Back to Dashboard**

### 1.3 Создайте OAuth Client ID

1. В боковом меню выберите **APIs & Services** → **Credentials**
2. Нажмите **Create Credentials** → **OAuth client ID**
3. Выберите **Application type:** `Web application`

4. **Настройте параметры:**

   **Name:** `OnepromptAI Web Client`

   **Authorized JavaScript origins:**
   ```
   http://localhost:5173
   http://localhost:3000
   ```
   *(добавьте ваш production URL, когда будете деплоить)*

   **Authorized redirect URIs:**
   ```
   http://localhost:5173
   http://localhost:3000
   ```
   *(добавьте ваш production URL, когда будете деплоить)*

5. Нажмите **Create**

6. **Сохраните Client ID и Client Secret:**
   - Появится окно с вашими credentials
   - **Client ID** выглядит примерно так: `1234567890-abc123def456.apps.googleusercontent.com`
   - **Client Secret** выглядит примерно так: `GOCSPX-abc123def456`
   - Скопируйте эти значения в безопасное место

---

## 🔧 Шаг 2: Настройка Backend

### 2.1 Обновите файл `backend/.env`

Откройте файл `backend/.env` и замените placeholder значения:

```env
# Google OAuth - ЗАМЕНИТЕ НА РЕАЛЬНЫЕ ЗНАЧЕНИЯ
GOOGLE_CLIENT_ID=ваш_реальный_client_id_здесь
GOOGLE_CLIENT_SECRET=ваш_реальный_client_secret_здесь
```

**Пример:**
```env
GOOGLE_CLIENT_ID=1234567890-abc123def456.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=GOCSPX-abc123def456
```

---

## 🎨 Шаг 3: Настройка Frontend

### 3.1 Обновите файл `.env` (в корне проекта)

Откройте файл `.env` в корне проекта и замените placeholder:

```env
# Google OAuth - ЗАМЕНИТЕ НА РЕАЛЬНОЕ ЗНАЧЕНИЕ
VITE_GOOGLE_CLIENT_ID=ваш_реальный_client_id_здесь
```

**Пример:**
```env
VITE_GOOGLE_CLIENT_ID=1234567890-abc123def456.apps.googleusercontent.com
```

⚠️ **ВАЖНО:** Используйте ТОЛЬКО Client ID, БЕЗ Client Secret на frontend!

---

## 🚀 Шаг 4: Запуск приложения

### 4.1 Запустите Backend

```bash
cd backend
npm install  # если еще не установлены зависимости
npm run dev
```

Вы должны увидеть:
```
✅ Connected to Neon PostgreSQL
🚀 Server running on http://localhost:3001
```

### 4.2 Запустите Frontend

В новом терминале:

```bash
npm install  # если еще не установлены зависимости
npm run dev
```

Откройте браузер: http://localhost:5173

---

## ✅ Шаг 5: Тестирование

### 5.1 Проверка интеграции

1. Откройте http://localhost:5173
2. Нажмите на кнопку "Login" или "Sign Up"
3. В модальном окне вы должны увидеть кнопку **"Sign in with Google"**
4. Нажмите на кнопку Google
5. Выберите ваш Google аккаунт
6. Подтвердите разрешения (если требуется)
7. Вы должны быть автоматически залогинены

### 5.2 Проверка в базе данных

Проверьте, что пользователь создан в базе данных:

```bash
cd backend
npx prisma studio
```

Откроется Prisma Studio → выберите таблицу `users` → проверьте:
- ✅ Ваш email
- ✅ `google_id` заполнен
- ✅ `provider` = "google"
- ✅ `picture` содержит URL аватара

---

## 🐛 Troubleshooting (Решение проблем)

### Проблема 1: "redirect_uri_mismatch"

**Ошибка:**
```
Error: redirect_uri_mismatch
```

**Решение:**
1. Проверьте, что URL в Google Console точно совпадает с вашим локальным URL
2. URL должен включать протокол (`http://`) и порт (`5173`)
3. Не должно быть слэша в конце: `http://localhost:5173` ✅ (не `http://localhost:5173/`)
4. После изменений подождите 5 минут для применения

### Проблема 2: Google button не отображается

**Возможные причины:**
1. **VITE_GOOGLE_CLIENT_ID не установлен:**
   - Проверьте файл `.env` в корне проекта
   - Перезапустите `npm run dev` после изменения `.env`

2. **Client ID неправильный:**
   - Убедитесь, что скопировали полностью
   - Должен заканчиваться на `.apps.googleusercontent.com`

### Проблема 3: CORS errors

**Ошибка в консоли браузера:**
```
Access to fetch at '...' has been blocked by CORS policy
```

**Решение:**
1. Проверьте `FRONTEND_URL` в `backend/.env`:
   ```env
   FRONTEND_URL=http://localhost:5173
   ```
2. Перезапустите backend сервер

### Проблема 4: "Invalid Google token"

**Ошибка:**
```
Error: Invalid Google token
```

**Решение:**
1. Проверьте, что `GOOGLE_CLIENT_ID` в `backend/.env` совпадает с frontend
2. Убедитесь, что Google project в режиме "Testing" включает ваш email в Test users
3. Попробуйте очистить кэш браузера и cookies

### Проблема 5: База данных не подключается

**Ошибка:**
```
❌ Failed to connect to database
```

**Решение:**
1. Проверьте `DATABASE_URL` в `backend/.env`
2. Убедитесь, что Neon проект активен
3. Проверьте, что миграция применена:
   ```bash
   cd backend
   node run-google-migration.js
   ```

---

## 📊 Шаг 6: Production Deploy

### 6.1 Обновите Google Cloud Console для Production

1. Вернитесь в **Google Cloud Console** → **Credentials**
2. Выберите ваш OAuth Client
3. Добавьте production URLs в:
   - **Authorized JavaScript origins:**
     ```
     https://ваш-домен.com
     ```
   - **Authorized redirect URIs:**
     ```
     https://ваш-домен.com
     ```

### 6.2 Опубликуйте OAuth App

1. Перейдите в **OAuth consent screen**
2. Нажмите **Publish App**
3. Подтвердите публикацию

⚠️ **Примечание:** Если ваше приложение требует верификации Google (для production использования), это может занять несколько дней.

### 6.3 Обновите Environment Variables на Production

**Backend (Vercel/Railway/Render):**
```env
GOOGLE_CLIENT_ID=ваш_production_client_id
GOOGLE_CLIENT_SECRET=ваш_production_client_secret
FRONTEND_URL=https://ваш-домен.com
```

**Frontend (Vercel/Netlify):**
```env
VITE_GOOGLE_CLIENT_ID=ваш_production_client_id
VITE_API_URL=https://api.ваш-домен.com
```

---

## 🎉 Готово!

Теперь ваше приложение поддерживает вход через Google! 🚀

### Возможности:

✅ Регистрация через Google (автоматическое создание аккаунта)  
✅ Вход через Google (для существующих пользователей)  
✅ Автоматическая привязка к существующему email-аккаунту  
✅ Синхронизация аватара из Google  
✅ Приветственный бонус для новых пользователей ($1.00)

### Дополнительные фичи:

- Пользователи могут использовать как Google OAuth, так и email/password
- Аватар автоматически обновляется при каждом входе
- Email автоматически верифицируется для Google пользователей

---

## 📚 Дополнительные ресурсы

- [Google OAuth 2.0 Documentation](https://developers.google.com/identity/protocols/oauth2)
- [@react-oauth/google Docs](https://www.npmjs.com/package/@react-oauth/google)
- [Neon PostgreSQL Docs](https://neon.tech/docs)
- [Prisma Documentation](https://www.prisma.io/docs)

---

## 💬 Поддержка

Если у вас возникли проблемы:
1. Проверьте секцию Troubleshooting выше
2. Проверьте консоль браузера (F12) на ошибки
3. Проверьте логи backend сервера

**Happy coding! 🚀**
